gnome = import('gnome')

# Resources
resources_xml = join_paths(meson.source_root(), 'data', 'com.github.gffranco.openxboxclient.gresource.xml')
resources_src = gnome.compile_resources('openxboxclient-resources',
  resources_xml,
)
