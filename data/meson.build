gnome = import('gnome')

# Resources
resources_xml = join_paths(meson.source_root(), 'data', 'com.github.gffranco.OpenXboxClient.gresource.xml')
resources_src = gnome.compile_resources('OpenXboxClientResources',
  resources_xml,
)
